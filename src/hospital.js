/* a contract to manage fights between players and dronestrikes */


var Hospital = {};

Hospital.resolvePlayerFight = function(attackingPlayer, defendingPlayer) {

    // return result of the fight
    // the losing players position must be updated (i.e. move back to central location)
}
