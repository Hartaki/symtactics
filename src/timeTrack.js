/* time track abstraction */

var TimeTrack = {};

TimeTrack.currentRound = 1;


TimeTrack.nextRound = function() {

    this.currentRound += 1;

    // rebalances -> contact symtactics game contract and trigger rebalancing
    
    if (currentRound == 4) {}
    if (currentRound == 7) {}
    if (currentRound == 9) {}


    if (currentRound == 10) { 
        // last round!
    }
    if (currentRound == 11) {
        // call symtactics contract and end the game
    }
}

